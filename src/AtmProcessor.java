/**
 * AtmProcessor class contains all logics for withdrawal, deposit, balance checking..
 */
public class AtmProcessor {
    /**
     * Deposit money from ATM
     * - If successful return the withdrawal amount (0 < amount <= 1000000 usd)
     * - If ATM insufficient money return -1
     * - If other error occurred return -2
     * @param amount a double number represent the withdrawal amount user inputted
     * @return a double number represent successfully withdrawal amount
     */
    public double withdrawal(double amount) {
        //@TODO các logic kết nối máy chủ ngân hàng và thực hiện rút tiền, sau đó ra lệnh ATM nhả tiền
        return 0;
    }
}
